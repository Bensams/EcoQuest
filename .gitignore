/target
**/target
.env
# Vite reads .env.local / .env.*.local for machine-specific overrides.
.env.local
.env.*.local
web/.env.local
web/.env.*.local
# Self-signed dev certificates. Never commit private keys.
web/.certs/
*.pem
*.log
node_modules/
web/dist/
web/node_modules/
# web/public/game/ is generated by `npm run build:wasm`, but is committed on
# purpose: it lets Vercel build the client with plain Node, without a Rust
# toolchain. CI rebuilds it and fails if the committed copy has drifted.
blockchain/target/
.DS_Store
# Claude Code local worktrees and session state.
.claude/
# Vercel CLI writes project ids and build output here.
.vercel/

.vercel
.env*
